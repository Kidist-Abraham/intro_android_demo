version: 0.2

phases:
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew assembleDebug
      - ls app/build/outputs/apk
artifacts:
  files: 
     - './app/build/outputs/apk/*.apk'
  discard-paths: yes
